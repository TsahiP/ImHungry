<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imhungry3">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="LOCATION_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.Setting.SettingActivity"></activity>
        <activity android:name=".Activity.Setting.SplashDeleteAccActivity" />
        <activity android:name=".Activity.Register.PreRegisterActivity" />
        <activity android:name=".Activity.Chat.ChatActivity" />
        <activity android:name=".Activity.WorkShop.EditWkShopActivity" />
        <activity android:name=".Activity.Search.ResultSearchActivity" />
        <activity android:name=".Activity.Search.SearchActivity" />
        <activity android:name=".Activity.Category.RecipeListActivity" />
        <activity android:name=".Activity.Category.RecipeCategoryActivity" />
        <activity android:name=".Activity.Recipe.CreateRecipeActivity" />
        <activity android:name=".Activity.Recipe.RecipeDisplayActivity" />
        <activity android:name=".Activity.UserPlace.PersonalAreaActivity" />
        <activity android:name=".Activity.WorkShop.DisplayWorkShopActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activity.WorkShop.CreateWorkshopActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Activity.WorkShop.WorkShopActivity" />
        <activity android:name=".Activity.HomeActivity" />
        <activity android:name=".Activity.Register.RegisterActivity" />
        <activity android:name=".Activity.Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>